# Frontend env — copy to .env.local for local dev, set in Vercel dashboard for prod
# Local dev: leave empty → uses Vite proxy to http://localhost:8000
# Vercel prod: set to your Render backend URL (e.g. https://vakrag.onrender.com)
VITE_API_URL=
